name            = "cajamercadolimpio"
main            = "worker.js"
compatibility_date = "2025-11-21"

# Reconciliación automática diaria GAS → Supabase (06:00 UTC = 03:00 AR)
[triggers]
crons = ["0 6 * * *"]
